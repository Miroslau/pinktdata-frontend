server {
  listen $PORT;

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
  }

  location ~ ^/api {
		      rewrite ^/api/(.*) /$1 break;
	        proxy_pass ${HEROKU_APP_BACKEND_URL};
	      }
}
