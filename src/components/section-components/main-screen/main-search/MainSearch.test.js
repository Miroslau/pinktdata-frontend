// import getCities from './MainSearch';

// global.fetch = jest.fn(() => Promise.resolve({
//   json: () => Promise.resolve({ cities: ['minsk', 'gomel', 'mogileu', 'moscow', 'miami'] }),
// }));
// it('searchLocation shoud return cities', () => {
//   const data = getCities('m');
//   expect(data).toBe(['minsk', 'gomel', 'mogileu', 'moscow', 'miami']);
//   expect(fetch).toHaveBeenCalledTimes(1);
// });
